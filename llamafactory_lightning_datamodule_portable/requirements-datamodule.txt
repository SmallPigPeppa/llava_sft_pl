# Core runtime dependencies for the portable parquet-only DataModule slice.
# Keep versions aligned with the uploaded LlamaFactory codebase.
torch>=2.4.0
torchvision>=0.19.0
torchaudio>=2.4.0
transformers>=4.55.0,<=5.2.0,!=4.52.0,!=4.57.0
datasets>=2.16.0,<=4.0.0
pyarrow
numpy
fsspec
huggingface-hub
packaging
typing-extensions

# Lightning. Either this package or pytorch-lightning is accepted by the module.
lightning>=2.0.0

# LlamaFactory data/collator helpers imported by this slice.
peft>=0.18.0,<=0.18.1
accelerate>=1.3.0,<=1.11.0
trl>=0.18.0,<=0.24.0
omegaconf

# Common tokenizer / multimodal dependencies. Some are only needed for the
# corresponding model or data type.
sentencepiece
tiktoken
pillow
av>=10.0.0,<=16.0.0
